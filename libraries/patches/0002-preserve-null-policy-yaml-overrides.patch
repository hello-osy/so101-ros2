diff --git a/src/lerobot/configs/parser.py b/src/lerobot/configs/parser.py
--- a/src/lerobot/configs/parser.py
+++ b/src/lerobot/configs/parser.py
@@ -53,6 +53,8 @@ def _flatten_to_cli_args(d: dict, prefix: str = "") -> list[str]:
             value = str(value).lower()
         if isinstance(value, dict):
             args.extend(_flatten_to_cli_args(value, full_key))
-        elif value is not None and not isinstance(value, list):
+        elif value is None:
+            args.append(f"--{full_key}=null")
+        elif not isinstance(value, list):
             args.append(f"--{full_key}={value}")
     return args
