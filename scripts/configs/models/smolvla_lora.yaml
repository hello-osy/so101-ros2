# 이 파일을 복사해 policy/LoRA 값을 바꾸면 모델 실험을 분리할 수 있다.
policy:
  path: lerobot/smolvla_base
  input_features: null
  output_features: null
  device: cuda
  use_amp: true
  push_to_hub: false
peft:
  method_type: LORA
  r: 16
  lora_alpha: 16
